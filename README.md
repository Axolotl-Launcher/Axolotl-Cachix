# Axolotl-Cachix

Prebuilt [Axolotl](https://github.com/Mystic-Stars/Axolotl) artifacts are pushed
to the `https://axolotl-launcher-git.cachix.org` Cachix cache. The table below is
updated automatically by the [`nix-cache` workflow](.github/workflows/nix-cache.yml).

<!-- BEGIN CACHED-COMMITS -->
_Not generated yet. Trigger the workflow to populate this table._
<!-- END CACHED-COMMITS -->
